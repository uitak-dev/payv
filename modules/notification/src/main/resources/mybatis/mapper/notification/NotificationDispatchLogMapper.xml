<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.payv.notification.infrastructure.persistence.mybatis.mapper.NotificationDispatchLogMapper">

    <insert id="insertIgnore" parameterType="com.payv.notification.infrastructure.persistence.mybatis.record.NotificationDispatchLogRecord">
        insert into notification_dispatch_log (
            dispatch_key,
            owner_user_id,
            notification_type,
            created_at
        ) values (
            #{dispatchKey},
            #{ownerUserId},
            #{notificationType},
            now()
        )
        on conflict (dispatch_key) do nothing
    </insert>

</mapper>

<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.payv.iam.infrastructure.persistence.mybatis.mapper.UserMapper">

    <resultMap id="UserRecordMap" type="com.payv.iam.infrastructure.persistence.mybatis.record.UserRecord">
        <id property="userId" column="user_id"/>
        <result property="email" column="email"/>
        <result property="passwordHash" column="password_hash"/>
        <result property="displayName" column="display_name"/>
        <result property="isActive" column="is_active"/>
        <result property="createdAt" column="created_at"/>
        <result property="updatedAt" column="updated_at"/>
    </resultMap>

    <insert id="upsert" parameterType="com.payv.iam.infrastructure.persistence.mybatis.record.UserRecord">
        INSERT INTO users (
            user_id,
            email,
            password_hash,
            display_name,
            is_active,
            created_at,
            updated_at
        ) VALUES (
            #{userId},
            #{email},
            #{passwordHash},
            #{displayName},
            #{active},
            COALESCE(#{createdAt}, NOW()),
            NOW()
        )
        ON CONFLICT (user_id)
        DO UPDATE SET
            email = EXCLUDED.email,
            password_hash = EXCLUDED.password_hash,
            display_name = EXCLUDED.display_name,
            is_active = EXCLUDED.is_active,
            updated_at = NOW()
    </insert>

    <select id="selectById" resultMap="UserRecordMap">
        SELECT
            user_id,
            email,
            password_hash,
            display_name,
            is_active,
            created_at,
            updated_at
        FROM users
        WHERE user_id = #{userId}
        AND is_active = TRUE
    </select>

    <select id="selectByEmail" resultMap="UserRecordMap">
        SELECT
            user_id,
            email,
            password_hash,
            display_name,
            is_active,
            created_at,
            updated_at
        FROM users
        WHERE email = #{email}
        AND is_active = TRUE
    </select>

    <select id="countByEmail" resultType="int">
        SELECT COUNT(*) FROM users
        WHERE email = #{email} AND is_active = TRUE
    </select>

</mapper>

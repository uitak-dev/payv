<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:security="http://www.springframework.org/schema/security"
       xsi:schemaLocation="
           http://www.springframework.org/schema/beans
           http://www.springframework.org/schema/beans/spring-beans.xsd
           http://www.springframework.org/schema/security
           http://www.springframework.org/schema/security/spring-security.xsd">

    <security:http auto-config="false"
                   use-expressions="true"
                   entry-point-ref="iamAuthenticationEntryPoint">
        <security:csrf disabled="true"/>

        <security:intercept-url pattern="/assets/**" access="permitAll"/>
        <security:intercept-url pattern="/login" access="permitAll"/>
        <security:intercept-url pattern="/perform_login" access="permitAll"/>
        <security:intercept-url pattern="/api/iam/auth/signup" access="permitAll"/>
        <security:intercept-url pattern="/api/iam/auth/login" access="permitAll"/>
        <security:intercept-url pattern="/error/**" access="permitAll"/>

        <security:intercept-url pattern="/api/**" access="hasRole('USER')"/>
        <security:intercept-url pattern="/**" access="hasRole('USER')"/>

        <security:form-login login-page="/login"
                             login-processing-url="/perform_login"
                             username-parameter="email"
                             password-parameter="password"
                             authentication-success-handler-ref="iamAuthenticationSuccessHandler"
                             authentication-failure-handler-ref="iamAuthenticationFailureHandler"/>

        <security:logout logout-url="/logout"
                         success-handler-ref="iamLogoutSuccessHandler"
                         invalidate-session="true"
                         delete-cookies="JSESSIONID"/>

        <security:session-management>
            <security:concurrency-control max-sessions="1" expired-url="/login?expired=true"/>
        </security:session-management>

        <security:access-denied-handler ref="iamAccessDeniedHandler"/>
    </security:http>

    <bean id="iamPasswordEncoder" class="org.springframework.security.crypto.bcrypt.BCryptPasswordEncoder"/>

    <security:authentication-manager alias="authenticationManager">
        <security:authentication-provider ref="iamAuthenticationProvider"/>
    </security:authentication-manager>
</beans>

<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:context="http://www.springframework.org/schema/context"
       xmlns:tx="http://www.springframework.org/schema/tx"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xsi:schemaLocation="
         http://www.springframework.org/schema/beans   http://www.springframework.org/schema/beans/spring-beans.xsd
         http://www.springframework.org/schema/context http://www.springframework.org/schema/context/spring-context.xsd
         http://www.springframework.org/schema/tx      http://www.springframework.org/schema/tx/spring-tx.xsd">

    <!-- 1) properties -->
    <context:property-placeholder location="classpath:application.properties"/>

    <!-- 2) component scan: app-web 조립 컨텍스트에서 각 모듈 application/infrastructure 빈을 스캔 -->
    <context:component-scan base-package="
        com.payv.iam,
        com.payv.asset,
        com.payv.classification,
        com.payv.ledger,
        com.payv.transfer,
        com.payv.budget,
        com.payv.automation,
        com.payv.reporting,
        com.payv.notification"
    >
        <context:exclude-filter type="regex" expression="com\\.payv\\..*\\.presentation\\..*"/>
    </context:component-scan>

    <!-- 3) DataSource (HikariCP) -->
    <bean id="dataSource" class="com.zaxxer.hikari.HikariDataSource" destroy-method="close">
        <property name="driverClassName" value="${jdbc.driverClassName}"/>
        <property name="jdbcUrl" value="${jdbc.url}"/>
        <property name="username" value="${jdbc.username}"/>
        <property name="password" value="${jdbc.password}"/>

        <!-- 풀 튜닝 값(필요시 조정) -->
        <property name="maximumPoolSize" value="${hikari.maximumPoolSize:10}"/>
        <property name="minimumIdle"     value="${hikari.minimumIdle:2}"/>
        <property name="idleTimeout"     value="${hikari.idleTimeout:600000}"/>
        <property name="connectionTimeout" value="${hikari.connectionTimeout:30000}"/>
    </bean>

    <!-- 4) 트랜잭션 매니저 & @Transactional 활성화 -->
    <bean id="transactionManager" class="org.springframework.jdbc.datasource.DataSourceTransactionManager">
        <property name="dataSource" ref="dataSource"/>
    </bean>
    <bean id="transactionTemplate" class="org.springframework.transaction.support.TransactionTemplate">
        <property name="transactionManager" ref="transactionManager"/>
    </bean>
    <tx:annotation-driven transaction-manager="transactionManager"/>

    <!-- 5) MyBatis SqlSessionFactory -->
    <bean id="sqlSessionFactory" class="org.mybatis.spring.SqlSessionFactoryBean">
        <property name="dataSource" ref="dataSource"/>
        <property name="configLocation" value="classpath:mybatis/mybatis-config.xml"/>
        <property name="mapperLocations" value="classpath*:mybatis/mapper/**/*.xml"/>
    </bean>

    <!-- 6) Mapper interface scan (모듈별 mapper package 규칙 고정) -->
    <bean class="org.mybatis.spring.mapper.MapperScannerConfigurer">
        <property name="basePackage" value="
          com.payv.iam.infrastructure.persistence.mybatis.mapper,
          com.payv.asset.infrastructure.persistence.mybatis.mapper,
          com.payv.classification.infrastructure.persistence.mybatis.mapper,
          com.payv.ledger.infrastructure.persistence.mybatis.mapper,
          com.payv.transfer.infrastructure.persistence.mybatis.mapper,
          com.payv.budget.infrastructure.persistence.mybatis.mapper,
          com.payv.automation.infrastructure.persistence.mybatis.mapper,
          com.payv.reporting.infrastructure.persistence.mybatis.mapper,
          com.payv.notification.infrastructure.persistence.mybatis.mapper"/>
        <property name="sqlSessionFactoryBeanName" value="sqlSessionFactory"/>
    </bean>

    <!-- 7) security-context.xml import -->
    <import resource="classpath:spring/security-context.xml" />
    <import resource="classpath:spring/rabbit-context.xml" />

</beans>
